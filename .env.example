# Cache TTL in seconds (default: 300 = 5 minutes)
CACHE_TTL=300

# HTTP timeout in seconds (default: 8)
HTTP_TIMEOUT=8

# Number of retry attempts for HTTP requests (default: 3)
HTTP_RETRIES=3

# Maximum number of items to cache (default: 1000)
MAX_CACHE_SIZE=1000